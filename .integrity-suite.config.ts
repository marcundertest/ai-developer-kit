// Example configuration for Integrity Suite.
// You can disable or adjust specific rules here without editing the
// protected meta-test source code.  Only the `rules` section is currently
// supported but may be extended in the future.

export default {
  rules: {
    // 'typescript/no-allow-js': 'off', // turn off the allowJs check (some projects need it for legacy JS)
    // 'gitignore/allowed-roots': { extend: ['.terraform/', '*.pyc'] }, // extend gitignore roots with extra patterns
    // 'Should forbid em dash in Spanish source code comments': 'off', // disable an individual hygiene rule by matching the test title
  },
};
